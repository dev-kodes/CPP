using namespace std;
int main()  {
    // comparison operator
    // 1 or 0
    
    int a,b;
    cout<<"Enter the value of a: ";
    cin>>a;
    
    cout<<"Enter the value of b: ";
    cin>>b;
    
    // ==
    
    // if(a==b)
    // cout<<"yes";
    // else
    // cout<<"no";
    
    // > , < , >= , <= , !=
    
    if(a!=b)
    // why (4!=4 - becuase the statement is wroung)
    cout<<"yes";
    else
    cout<<"no";
};